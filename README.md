# Food-Card2
// แก้ไขในส่วนของฟังก์ชัน buildCards()
var baseUrl = "https://roongnapaprompan-star.github.io/Food-Card2/qr_food_cards_yummy_world.html";
var url = baseUrl + "#card=" + encodeURIComponent(FOODS[i].n);
